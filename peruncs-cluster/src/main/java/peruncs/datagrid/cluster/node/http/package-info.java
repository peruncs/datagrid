/// This package serves the node HTTP surface.
///
/// It owns the shared route table with its media types, the request
/// controller behind each route, and the header constants. The controller
/// remains responsible for the behavior behind them.
///
/// @since 1.0
package peruncs.datagrid.cluster.node.http;

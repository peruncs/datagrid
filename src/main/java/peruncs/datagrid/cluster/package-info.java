/// Data Grid cluster node: lifecycle, storage, and replication.
///
/// This package holds no types itself. The `node` package assembles and runs
/// one cluster node — foundation, fixed-topology managers, housekeeping,
/// configuration, and fatal errors. The `storage` package carries Store
/// binary movement and persisted index integration between nodes.
///
/// @since 1.0
package peruncs.datagrid.cluster;

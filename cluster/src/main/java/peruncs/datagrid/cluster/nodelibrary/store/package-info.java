/// This package adapts Eclipse Store to cluster duty.
///
/// It owns the cluster-aware storage manager with its shutdown handling, the
/// storage task executor, disk-space reading, file operations, the storage
/// limit gate, and the storage health check.
///
/// @since 1.0
package peruncs.datagrid.cluster.nodelibrary.store;

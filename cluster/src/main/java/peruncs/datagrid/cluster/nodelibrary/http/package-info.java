/**
 * This package serves the node HTTP surface.
 *
 * <p>It owns the shared route table with its media types, the request
 * controller behind each route, and the header constants. The controller
 * remains responsible for the behavior behind them.</p>
 *
 * @since 1.0
 */
package peruncs.datagrid.cluster.nodelibrary.http;
